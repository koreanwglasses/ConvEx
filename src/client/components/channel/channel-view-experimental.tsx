import React from "react";
import { Panel, PanelContainer } from "../helpers/panels";

export const ChannelViewExperimental = ({
  channelId,
  guildId,
}: {
  channelId: string;
  guildId: string;
}) => (
  <PanelContainer>
    <Panel></Panel>
    <Panel></Panel>
  </PanelContainer>
);
