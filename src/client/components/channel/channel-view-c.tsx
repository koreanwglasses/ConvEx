import React from "react";


/**
 * - Displays a line graph representation of message toxicity
 * - Messages are stacked on top of each other (i.e. y axis is not absolute time)
 */
export const ChannelViewC = ({
  channelId,
  guildId,
}: {
  channelId: string;
  guildId: string;
}) => <i>WIP</i>;
